# Okta Provider Configuration for TEST App

# Okta Organization Name
okta_org_name = "dev-52505730"

# Okta Base URL
okta_base_url = "okta.com"

# Okta API Token (should be set via environment variable OKTA_API_TOKEN)
# okta_api_token = "your-api-token"

# App configuration path (derived from folder structure)
app_config_path = "apps/DIV1/TEST/dev"

# Environment (derived from folder name)
environment = "dev" 